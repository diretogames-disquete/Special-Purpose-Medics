/* Maps the body-diagram image ids the casualty panel asks for
   (window.__resources[...]) to the unpacked PNG files. Mirrors the
   resource map the original bundle injected at runtime. */
window.__resources = {
  bodyFront: "assets/img/bodyFront.png",
  bodyBack: "assets/img/bodyBack.png"
};
