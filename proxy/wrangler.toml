name = "spm-narration-proxy"
main = "worker.js"
compatibility_date = "2024-11-01"

# --- Set these after deploy ---
# Secret (do NOT put the key here — use `wrangler secret put`):
#   wrangler secret put ANTHROPIC_API_KEY
#
# Optional vars (safe to commit): lock the proxy to your site + a shared token.
[vars]
# ALLOWED_ORIGIN = "https://diretogames-disquete.github.io"
# SHARED_TOKEN   = "choose-a-long-random-string"
# MODEL          = "claude-haiku-4-5-20251001"
